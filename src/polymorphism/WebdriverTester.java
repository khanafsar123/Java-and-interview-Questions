/*
package polymorphism;

public class WebdriverTester {
    public static void main(String[] args) {
        // everytime creating new instance of the object
        */
/*Safari safari=new Safari();
        safari.startBrower();
        safari.openBrowser();
        safari.closeBrowser();

        Chrome chrome=new Chrome();
        chrome.startBrower();
        chrome.openBrowser();
        chrome.closeBrowser();

        FireFox fireFox=new FireFox();
        fireFox.startBrower();
        fireFox.openBrowser();
        fireFox.closeBrowser();
*//*


        // easy is to create the array and access everythign from there
        // create objects

        // we can write like
        // FireFox fireFox=new FireFox();
        // Chrome chrome=new Chrome ();
        // Safari safari=new Safari();
        // WebDriver[] browser={fireFox, chromre, safari};
        WebDriver[] browsers={new Chrome(),new FireFox(), new Safari()};

        // access element using for loop
        for(WebDriver browser1:browsers){
            // call method
            browser1.startBrower();
            browser1.closeBrowser();

        }
    }
}
*/
